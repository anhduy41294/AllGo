<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="55dp"
    tools:context=".Views.Fragments.AddRoomFragment">

    <ImageView
        android:id="@+id/imgRoom"
        android:layout_marginTop="10dp"
        android:src="@drawable/waiting_room"
        android:layout_centerHorizontal="true"
        android:layout_width="50dp"
        android:layout_height="50dp" />

    <EditText
        android:id="@+id/edtRoomName"
        android:layout_below="@+id/imgRoom"
        android:layout_margin="8dp"
        android:hint="Room Name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <EditText
        android:id="@+id/edtRoomDescription"
        android:layout_below="@+id/edtRoomName"
        android:layout_margin="8dp"
        android:inputType="textMultiLine"
        android:hint="Room Description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />


    <Button
        android:id="@+id/btnSubmitRoom"
        android:layout_below="@+id/edtRoomDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Submit"
        style="@style/ButtonStyle"/>


</RelativeLayout>
